#!/bin/bash

export PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin

mkdir -p /root
cd /root
chmod 755 tf_build

# Run tf_build script without args, give the options available
echo "To start build, type:"
./tf_build

/bin/bash -i
